<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>I Love You ❤️</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:wght@400;600&display=swap" rel="stylesheet">
<style>
  :root{
    --pink:#ff1a75;
    --light:#ffd6e8;
  }
  *{box-sizing:border-box}
  body{
    margin:0;
    min-height:100vh;
    background:
      radial-gradient(800px 400px at 0% 0%, #ffe0ef, transparent 70%),
      radial-gradient(800px 400px at 100% 100%, #ffe0ef, transparent 70%),
      linear-gradient(180deg, #fff7fb 0%, #ffd6e8 100%);
    font-family:'Poppins', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
    overflow:hidden;
  }

  /* Floating heart confetti */
  .bg{
    position:fixed; inset:0; pointer-events:none; z-index:0; overflow:hidden;
  }
  .bg span{
    position:absolute; font-size:clamp(18px, 4vw, 34px); opacity:.9;
    animation: rise linear infinite;
  }
  .bg span:nth-child(1){left:6%;  animation-duration:12s; animation-delay:0s}
  .bg span:nth-child(2){left:14%; animation-duration:13s; animation-delay:1.2s}
  .bg span:nth-child(3){left:22%; animation-duration:11s; animation-delay:.6s}
  .bg span:nth-child(4){left:32%; animation-duration:12.5s; animation-delay:2.2s}
  .bg span:nth-child(5){left:47%; animation-duration:14s; animation-delay:.8s}
  .bg span:nth-child(6){left:58%; animation-duration:11.8s; animation-delay:1.8s}
  .bg span:nth-child(7){left:68%; animation-duration:13.2s; animation-delay:.4s}
  .bg span:nth-child(8){left:78%; animation-duration:12.3s; animation-delay:2.8s}
  .bg span:nth-child(9){ left:90%; animation-duration:15s;   animation-delay:1.1s}
  @keyframes rise{
    0%   {transform: translateY(100vh) rotate(0turn);   opacity: .0;}
    10%  {opacity: .95;}
    100% {transform: translateY(-20vh) rotate(1turn); opacity: .2;}
  }

  .wrap{
    position:relative; z-index:1;
    min-height:100vh; display:flex; align-items:center; justify-content:center;
    padding:24px;
  }

  .card{
    background:#fff;
    border-radius:28px;
    box-shadow: 0 10px 40px rgba(255, 26, 117, .25);
    padding: clamp(20px, 4vw, 44px);
    max-width: 820px; width:100%;
    text-align:center; position:relative;
  }

  h1{
    font-family: 'Pacifico', cursive;
    color: var(--pink);
    font-size: clamp(34px, 7.5vw, 64px);
    margin: 0 0 10px;
    animation: heartbeat 1.2s infinite ease-in-out;
  }
  @keyframes heartbeat {
    0% { transform: scale(1); }
    25% { transform: scale(1.06); }
    40% { transform: scale(1); }
    60% { transform: scale(1.06); }
    100% { transform: scale(1); }
  }

  p.note{
    margin: 8px 0 22px;
    color:#6a2945; font-weight:600;
  }

  .photo-wrap{
    position:relative; display:inline-block; margin: 8px auto 14px;
  }
  .photo{
    width: min(86vw, 420px);
    aspect-ratio: 1/1;
    object-fit: cover;
    border-radius: 24px;
    box-shadow: 0 14px 40px rgba(255, 26, 117, .35);
    border: 8px solid #fff;
    outline: 4px solid #ffc3da;
  }

  /* Cute rotating stickers around the photo */
  .sticker{
    position:absolute; font-size: clamp(28px, 7vw, 42px);
    filter: drop-shadow(0 6px 10px rgba(0,0,0,.1));
    animation: bob 3s ease-in-out infinite;
  }
  .s1{ top:-8%;  left:-6%;  animation-delay:0s;  transform: rotate(-12deg);}
  .s2{ top:-10%; right:-6%; animation-delay:.3s; transform: rotate(10deg);}
  .s3{ bottom:-10%; left:-6%; animation-delay:.6s; transform: rotate(8deg);}
  .s4{ bottom:-12%; right:-8%; animation-delay:.9s; transform: rotate(-8deg);}
  @keyframes bob{
    0%,100%{ transform: translateY(0) rotate(var(--rot,0deg)); }
    50%    { transform: translateY(-6px) rotate(var(--rot,0deg)); }
  }

  .back{
    margin-top: 18px;
    display:inline-block;
    background:#ff79a8; color:#fff; border:none; border-radius:12px;
    padding: 12px 18px; cursor:pointer; font-weight:600;
    box-shadow: 0 8px 22px rgba(255,121,168,.35);
    text-decoration:none;
  }
  .back:active{ transform: scale(.98); }
</style>
</head>
<body>

<!-- Floating background hearts & sparkles -->
<div class="bg" aria-hidden="true">
  <span>💖</span><span>💘</span><span>💝</span><span>✨</span><span>🌸</span>
  <span>💞</span><span>🌷</span><span>🫶</span><span>💗</span>
</div>

<div class="wrap">
  <div class="card">
    <h1>I LOVE YOU ❤️</h1>
    <p class="note">You make my world brighter every day. Thank you for being you. 💗</p>

    <div class="photo-wrap">
      <!-- ✅ Replace "us.jpg" with your actual file name/path if different -->
      <img class="photo" src="us.jpg" alt="Us together - my favorite photo 💞" />

      <!-- cute stickers around the photo -->
      <div class="sticker s1" style="--rot:-12deg">💐</div>
      <div class="sticker s2" style="--rot:10deg">🧸</div>
      <div class="sticker s3" style="--rot:8deg">🎀</div>
      <div class="sticker s4" style="--rot:-8deg">🌟</div>
    </div>

    <a class="back" href="index.html">← Back</a>
  </div>
</div>

</body>
</html>